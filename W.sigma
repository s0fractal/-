---
GLYPH: W
ENERGY: 1
SPECTRUM: [ts, rs, sh, dna]
SOURCE:
  - origin: null # Axiom: Pure Resource
PHYSICS:
  mass: 0.1kb
  entropy: Medium
  complexity: O(1)
---

# Witness (W) - Fork

Чисте розмноження ресурсу. Дублює аргумент для функції.
Аксіома форку.

## Wave Function

@[ts]
```typescript
export const W = <A, B>(f: (x: A) => (y: A) => B) => (x: A): B => f(x)(x);
```

@[rs]
```rust
pub fn W<A, B, F>(f: F, x: A) -> B 
where 
    F: Fn(A) -> Box<dyn Fn(A) -> B>,
    A: Clone
{
    f(x.clone())(x)
}
```

@[sh]
```bash
# W (Witness): Дублює потік
W() {
    tee >(cat)
}
```

@[dna]
```dna
W1
```
