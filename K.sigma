---
GLYPH: K
ENERGY: 1
SPECTRUM: [ts, rs, sh, dna]
SOURCE:
  - origin: null # Axiom: Pure Matter
PHYSICS:
  mass: 0.1kb
  entropy: Low
  complexity: O(1)
---

# Kestrel (K) - Constant

Чиста матерія вибору. Повертає перший аргумент, ігнорує другий.
Аксіома, що існує незалежно від руху (I).

## Wave Function

@[ts]
```typescript
export const K = <A, B>(x: A) => (y: B): A => x;
```

@[rs]
```rust
pub fn K<A, B>(x: A) -> impl Fn(B) -> A + Clone 
where A: Clone
{
    move |_| x.clone()
}
```

@[sh]
```bash
# K (Constant): Ігнорує потік, видає аргумент
K() {
    cat > /dev/null
    echo "$1"
}
```

@[dna]
```dna
K1
```
